server:
  port: 8080
cors:
  originPatterns: http://localhost:3000,http://localhost:8080,https://agymsistemas.com.br
security:
  jwt:
    token:
      secret-key: 553cr37
      expire-length: 36000000
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/rest_with_spring_boot?useTimezone=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      # hibernate:
      #  dialect: org.hibernate.dialect.MySQLDialect
      show-sql: false
  application:
    name: rest-with-java-sprint-boot 
springdoc:
  pathsToMatch: 
    - /auth/**
    - /api/**/v1/**
  swagger-ui:
    use-root-path: true